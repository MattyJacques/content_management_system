<!DOCTYPE html>
<html>
  <head>
    <title>ContentManagementSystem<%= ' - ' + yield(:title) if content_for?(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= yield(:script) %>
  </head>

  <body>

    <header style="height: 1em; padding: 1em; margin-bottom: 1em; background: #0099CC">
      ContentManagementSystem
      <% if session[:user_id].present? %>
        <span style="float: right;">
          User: <%= @username %> (<%= link_to('Logout', logout_path, method: :delete) %>)
        </span>
      <% end %>
    </header>

    <div id="content" style="min-height: 700px;">
      <%= render(partial: 'application/flash') %>
      <%= yield %>
    </div>

    <header style="height: 1em; padding: 1em; margin-top: 1em; background: #0099CC">
      Copyright <%= Time.now.year %>
    </header>

  </body>
</html>
